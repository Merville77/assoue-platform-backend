package com.assoue.platform.repository;

import com.assoue.platform.entity.GarageProfil;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GarageProfilRepository extends JpaRepository<GarageProfil, Long> {
}